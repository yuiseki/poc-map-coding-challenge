/** ms between each test result reveal — map animation must finish within this */
export const REVEAL_DELAY_MS = 800;

/** fitBounds / flyTo animation duration — keep shorter than REVEAL_DELAY_MS */
export const MAP_ANIM_DURATION_MS = 650;
